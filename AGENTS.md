# Repository Notes

- Product and code-facing terminology should intentionally use `Twitter`, `Tweet`, and `tweet`.
- Do not use `X` as the service name in comments, docs, logs, test names, or user-facing prose.
- Keep literal protocol/header/API field names unchanged when they are externally defined, such as `X-Misskey-Hook-Secret`, `X-Upload-Content-Type`, and `x-twitter-webhooks-signature`.
